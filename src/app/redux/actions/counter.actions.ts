import { createAction } from '@ngrx/store';

export const show = createAction('[Loader Component] show');
export const hide = createAction('[Loader Component] hide');
export const reset = createAction('[Loader Component] Reset');